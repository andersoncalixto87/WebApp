@model WebApp.Models.LoginViewModel

@{
    ViewData["Title"] = "Redefinir a senha";
}

<div class="row">
    <div class="col-md-12">
        <form asp-action="ResetPassword" class="col-md-4 form-shadow" style="margin:0 auto">
            <div class="form-group">
              <h2>Redefinir senha</h2>
                
            </div>
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="UsernameOrEmail" class="control-label"></label>
                <input asp-for="UsernameOrEmail" class="form-control" placeholder="Usuário ou E-mail" />
                <span asp-validation-for="UsernameOrEmail" class="text-danger"></span>
            </div>
            
            <div class="form-group">
                <input type="submit" value="Enviar senha" class="btn btn-primary btn-block"  />
                
            </div>
            
           
        </form>
    </div>
</div>